eureka.client.service-url.defaultZone=http://${REGISTRY_HOST:localhost}:${REGISTRY_PORT:8761}/eureka/
eureka.client.service-url.healthcheck.enabled=true
eureka.client.enabled=true
eureka.instance.prefer-ip-address=true

server.port=8086
spring.application.name=${SPRINGBOOT_APP_NAME:renting-service}

#axon server config
#axon.axonserver.servers=${AXON_HOST:localhost}
#axoniq.datasource.eventstore.url=jdbc:postgresql://localhost:5432/service-db?useSSL=false
#axoniq.datasource.eventstore.username=postgres
#axoniq.datasource.eventstore.password=mocanpass

axon.axonserver.servers=${AXON_HOST:localhost}
